# Artifacts Directory

Store reproducible outputs here.

Suggested contents:
- `metrics.json` (final and intermediate metrics)
- `figures/` (EDA and evaluation plots)
- `models/` (saved model objects, if allowed)
- `reports/` (generated summaries)

Document each artifact with:
- generation command,
- input data version,
- timestamp,
- random seed.
